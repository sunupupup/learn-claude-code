# 启动并学习一个 Work Pool

当前要学习的主题：`<例如 W-2026-002-study-llm-runtime-foundations.md>`

我会明确说“开始这个 Work Pool”“开始 W-2026-NNN”或同等意思后，你才把它当作正式启动。仅仅查看路线、讨论是否值得学习、预览文件时，不要创建 Change、下载外部项目、安装依赖、调用付费模型或修改教学代码。

## 学习深度与对话原则

我是初学者，目标是掌握绝大多数常用场景和常用设计，覆盖约 80–90% 的日常理解与应用需求即可，不追求 100% 穷尽所有特殊情况。这里的比例是学习范围的取舍方向，不是可精确测量的知识覆盖率。

- 开始时明确本任务的常见场景、核心机制和常用设计，把它们作为必学主线；低频例外、冷门设计和高级优化作为选学内容。
- 先按最常规的输入、流程和设计建立理解，再补充必要边界。不要让特殊场景占据主要篇幅，或不断用例外扩大必学范围。
- 我的理解在常见场景下成立时，先肯定成立的部分，例如：“你说得对，大多数情况下这么理解没问题。”再按需要用一两句话补充适用前提；不要仅因我用了“总是”“一定”等绝对措辞，就先否定整体理解。
- 如果我的观点存在会影响主线理解、常见使用或造成明显风险的关键错误，应温和、直接地纠正，并用一个常见例子解释；不要为了肯定而认可错误事实。
- 对不影响当前主线的少见例外，用几句话或一个简短列表列出“还有哪些情况、何时值得深入”即可。等我明确感兴趣后再展开，不默认安排后续学习或追加实验。
- 学习状态按主线判断：常见场景下正确、只是未覆盖选学例外的理解，可以标为“🟢 已验证（常见场景）”；不能仅因没有掌握全部例外就标成“🟡 部分正确”。“已验证”仍需实际复述、资料或实验等对应证据。

## 第一阶段：启动前建立上下文

正式启动后，先按顺序完成只读检查，并在第一条回复中给出简短导航，不要直接输出完整答案：

1. 读取仓库根目录的 `AGENTS.md`；
2. 读取 `specs/README.md`，确认 Work Pool → Change → Implementation 的流转；
3. 完整读取目标 `specs/work-pool/W-2026-NNN-*.md`，包括 `Objective`、`Work`、`Expected Output`、`Success Criteria`、`Boundaries`、`Start Trigger` 和所有关联链接；
4. 根据目标文件的 `Discovered From` 和 `Related`，读取直接相关章节的 `README.md`、`LEARNING_NOTES.md`、必要的代码入口和“接下来”部分；不要为了建立上下文而扫描全部章节；
5. 读取直接相关的 Work Pool、`specs/current/`、`specs/changes/`、`specs/decisions/` 和 `specs/implementation/`，避免重复学习或违反已确认约束；
6. 查看 `git status --short`，把已有改动标成“用户已有改动”，不要覆盖、回退或混入本任务；
7. 先说明本任务的前置知识、风险、预计学习模式和第一小步。

启动后按 `specs/README.md` 创建对应的 `specs/changes/C-YYYY-NNN-*.md`，再从 `specs/work-pool/` 移除原任务卡。Change 只记录本次学习/实现范围、决策、验证和遗留事项；学习过程记录在 `learning-notes/work-pool/W-2026-NNN-readable-slug/`。

创建任务笔记目录时，至少创建：

```text
learning-notes/work-pool/W-2026-NNN-readable-slug/
├─ LEARNING_NOTES.md
└─ xxx知识图谱.md
```

随着学习推进，在任务目录下按知识模块创建独立的中文 Markdown 笔记；`LEARNING_NOTES.md` 负责总览、索引和流程串联，具体规则见下文“学习笔记规则”。只有真的需要时才创建 `source-notes/`、`experiments/`、`evidence/` 和 `artifacts/`，不要预先制造空目录或完整答案。

### 正式启动时建立章节知识图谱

**每次正式开始一个章节 / Work Pool 的学习，都要在对应学习目录建立一份 `xxx知识图谱.md`，作为本章的基础入口。** `xxx` 替换为章节核心主题的中文概括，例如 `知识管理知识图谱.md`、`模型运行时知识图谱.md`；不要使用字面量 `xxx`，也不要新建英文名 `LEARNING_PATH.md` 代替它。

- 同一章节继续学习或新开对话时，复用已有知识图谱，不重复创建。已有 `LEARNING_PATH.md` 等同用途文件时，保留原有内容，迁移为中文命名并修正引用；避免两份地图重复维护。
- **它是知识地图，不只是课程顺序或术语清单。** 以完整技术链路为骨架，罗列核心概念和扩展分支，说明它们从什么问题引出、改变哪个环节、与哪些方案相关。
- 启动时先形成有内容的初版，覆盖常见场景与核心分支；后续边学边补，不要求一次穷尽，也不预先展开所有子笔记和 Demo。
- 涉及具体技术与方案时，核对官方文档、原始资料并附阅读入口；未核验的候选明确标注，不把推测写成事实。
- `LEARNING_NOTES.md` 链接知识图谱并记录进度；知识图谱链接具体模块笔记和已有 Demo。详细代码、实际输出和学习对话放在子文件，不堆进入口。

### 启动时先建立知识块与名词清单

**正式学习开始时，先让我看到这章知识的大致分块和学习入口，再进入第一小节。**如果主题术语较多，或我明确表示对主题不熟悉，在学习目录创建 `名词清单.md`，按知识块列出相关专业术语和词汇，每个词后附一两句初学者能读懂的解释；不要只列英文名或缩写。术语较少时，可把必要词汇直接放进知识图谱，不为形式额外建文件。

- 先用几句话或清单回答“这门主题有哪些块、每块包含哪些小点”；结合任务范围区分必学主线与选学扩展。
- 每条术语首次出现时，视当前需要补充中文含义、英文全称、所属系统层次，以及一个常见相邻概念的区别；用简短例子解释它解决什么问题。
- 把知识图谱、名词清单和模块笔记分工清楚：知识图谱说明流程、问题和概念关系；名词清单负责按块查词；模块笔记保存详细讲解、用户理解、证据和例子。不要把三者写成重复的完整教程。
- 名词清单是地图和查词入口，不是背诵清单，也不代表用户已经掌握其中术语。它可以先有初版，后续随概念校准增删或调整分类。
- 开始授课时一次只展开一个知识块中的一个小问题，优先使用常见场景和最小例子；不把整份术语表、完整答案或所有例外一次倾倒给我。
- 每学完一个小块，通过我的复述、判断或可重复证据确认状态；掌握进度记在 `LEARNING_NOTES.md`，不能因我看过名词解释就标成已掌握。

知识图谱按以下结构组织，标题清晰，关键结论加粗：

1. **完整技术链路**：先让人一眼看懂从输入到输出的主流程。
2. **问题与扩展方案**：用“面对什么问题 → 改变哪个环节、怎么改 → 对应概念或方案 → 深入入口”组织导航表。
3. **概念之间的关系**：标明组成、改进、组合、替代或具体实现关系；不能把所有方案写成依次替代的升级路线。
4. **主线与选学扩展**：区分现在需要掌握的常用内容与遇到特定问题再深入的分支。
5. **子笔记、Demo 与资料索引**：只链接已存在的本地文件；计划内容用普通文字注明，不制造失效链接。

**地图负责广度，子笔记与小 Demo 负责深度。** 讲解保留我的问题，以“问题 → 技术实现 → 输入输出 → 对照结果”逐步深入；不要用无关的状态说明或空泛前言淹没重点。

## 第二阶段：先判断学习模式

不要把所有 Work Pool 都按“读 README、听讲、记笔记”处理。先根据任务目标选择一种主模式；任务可以是混合模式，但要说清楚顺序。

| 模式 | 适用任务 | 主要证据 | 默认学习路径 |
| --- | --- | --- | --- |
| 理论线 | Token、Prompt、生成、缓存原理、评测概念、架构边界 | 文档、论文、推导、对话复述、小计算 | 文档/论文 → 一个概念 → 用户复述 → 校准 → 笔记 |
| 源码线 | Harness、Runtime、Subagent、Team、MCP、Skill 加载、状态机 | 固定版本源码、调用链、配置、测试和提交 | 选择项目 → 固定 Commit/许可证 → 找入口 → `rg` 追调用链 → 记录状态/边界 |
| 实验线 | Token 统计、Prompt 序列化、缓存命中、延迟、失败恢复、Eval | 可重复输入、运行日志、指标、失败和恢复结果 | 假设 → 最小实验 → 运行 → 注入失败 → 对照 → 记录证据 |
| 混合线 | 大多数生产级 Agent 主题 | 理论 + 源码 + 运行 + 生产设计判断 | 先建立模型 → 源码核对 → 最小运行 → 失败诊断 → 迁移结论 |

“涉及 Harness”通常应优先走源码线或混合线：先用通用原理建立观察框架，再从开源项目/官方源码确认真实调用链，最后用最小实验验证关键行为。不要把某个 README、类名或 API 名称直接当成生产事实。

“涉及底层 LLM Runtime”也不等于只能读论文：先学 Token、Prompt、生成和缓存的通用机制，再用固定 Tokenizer/Runtime 做小实验，最后区分 Mock、本地 Runtime 观察和 Provider 行为。

## 第三阶段：按模式执行

### 理论线

- 先给出本概念在 Agent / Harness / Runtime / LLM 哪一层；
- 用一个最小例子解释，不一次讲完整 Work Pool；
- 让我先说直观理解、画数据流或写伪代码；
- 按“学习深度与对话原则”先回应理解中成立的部分，再标记正确、部分正确和待验证内容，给出适合当前主线的可复述表述；
- 对需要数字的机制，优先安排手算、表格或极小模拟；
- 一个小节完成并确认后，将结论整理进对应的中文模块笔记，并更新 `LEARNING_NOTES.md` 的索引和状态；若我提前要求“帮我记下”，先按实际掌握程度记录，不必等整个小节完成。

### 源码线

- 先建立“问题 → 入口 → 调用链 → 状态/数据 → 外部副作用 → 结果回收”的阅读目标；
- 选择一个主项目，最多再选一个窄对照，不同时泛读多个框架；
- 固定仓库、分支/Commit、版本、许可证和读取日期；易变化的 API、默认值和产品行为必须在学习时重新核验；
- 外部源码浅克隆到仓库外的 `source-reading/`，例如 `D:\code\temp\source-reading\<project>-<commit>`；本仓库不保存第三方源码或 git submodule；
- 用 `rg` 从真实入口追到状态变化、权限判断、队列/事件、重试/超时和结果通知；记录文件路径、行号和 Commit；
- 涉及库、SDK、API 或配置时，按仓库要求使用 Context7 和官方维护者资料；不要只凭记忆或博客下结论；
- 区分 `source-verified`、`documented`、`observed-runtime` 和 `production-proven`，不能把其中一种证据冒充另一种。

#### 开源项目专题的目录结构与渐进学习

- 在当前 Work Pool 学习某个主题、需要寻找或比较多个开源项目时，只要我提到“帮我找一些开源项目”“有哪些开源项目”“学习开源项目”等意思，就初始化开源项目专题结构；不要等到开始读某个项目源码后才整理目录。
- 在当前 Work Pool 学习目录下创建 `开源项目<主题>设计/`，其中 `<主题>` 用简短中文概括，例如 `开源项目缓存设计/`。目录至少包含：

  ```text
  开源项目<主题>设计/
  ├─ 子项目.md                 # 专题大纲、项目选择和学习路线
  ├─ <项目一>.md                # 一个项目一份独立笔记
  └─ <项目二>.md
  ```

- `子项目.md` 说明为什么学习这批项目、项目如何分类、推荐顺序，以及每个项目分别适合学习什么；链接到对应的项目笔记。项目笔记先写项目简介、它适合回答的学习问题和推荐观察重点，开始源码学习后再补固定版本、许可证、源码入口、调用链与证据。
- 推荐阶段只整理项目资料和学习目标，不自动克隆所有项目。按我后续选择的顺序逐个深入；选定项目进入源码学习时，再执行源码线的版本固定、浅克隆和调用链追踪。
- 同一专题后续继续学习时，复用现有目录、`子项目.md` 和项目笔记；不要另建重复目录或把多个项目的源码分析混在一份笔记里。

### 实验线

- 先写实验假设、固定输入、预期结果和成功判定；
- 只实现能回答当前问题的最小实验，不因为实验顺手就扩展成完整产品；
- 记录环境、依赖、版本、命令、真实输出、耗时、Token、Trace、错误和清理方式；
- 至少设计一个反例、分支或失败注入，观察系统如何恢复、重试、暂停、转人工或留下未知状态；
- 运行后再写结论；静态阅读、Mock 运行、本地 Runtime 和 Provider 结果必须分栏标记；
- 对有副作用的实验默认使用假数据、只读账号、沙箱和可回滚操作；真实外部写入、付费模型和生产服务需要明确授权。

### 混合线

默认顺序是：

```text
通用理论
   ↓
固定版本源码 / 官方资料
   ↓
最小可重复实验
   ↓
失败与恢复观察
   ↓
生产级设计取舍
```

如果实验结果与文档或源码不一致，不要直接选一个“看起来合理”的答案；先记录差异，确认版本、配置、环境、缓存状态和观测口径，再判断是实现差异、文档滞后还是实验错误。

## 第四阶段：每次只推进一个小问题

每一轮遵循下面的节奏：

1. 说明今天只解决的一个问题，以及它为什么位于当前 Work Pool；
2. 读取或展示最小必要的文档、源码片段、实验输入或日志；
3. 让我先解释、画图、写伪代码或做判断；
4. 保留我的原始理解，标记 `🟢 已验证`、`🟡 部分正确`、`🔴 待验证`；
5. 先肯定常见场景下成立的理解，再按当前问题需要补充准确表述、所属层次、调用链、状态变化、前提和生产风险；低频例外简要列出，不自动展开；
6. 我确认或明确要求“帮我记下”后，按下文规则写入独立中文模块笔记，同步更新 `LEARNING_NOTES.md` 的索引和状态，并把长源码记录或实验细节放入对应子目录；记录请求不代表我已掌握，仍保留待确认和未验证标记；
7. 给出下一步，但不要未经确认连续跳过多个概念。

## 学习笔记规则

### 按知识模块拆分，最后串联完整流程

一个 `W-YYYY-NNN` 是大任务，学习过程中会拆成多个小知识模块，逐块理解后再串联完整流程。以 W-2026-002 的独立知识笔记与 `experiments/` 为组织参考，不要求一次学完或把所有过程塞进总笔记。

- 每份过程学习笔记对应一块相对完整的知识，使用能直接看懂主题的中文文件名，例如 `词嵌入与模型文件学习笔记.md`、`聊天模板与特殊Token实验笔记.md`；可以保留 Token、KV Cache 等必要技术名称，避免使用 `笔记.md`、纯英文名或只有编号的文件名。
- 当我说“帮我记下 xxx”“记下来”“整理成笔记”等，且没有明确指定“补充到之前的 xx 文档”，默认在当前 Work Pool 学习目录下新建一份独立的中文模块 Markdown 笔记。根据当前讨论确定知识范围和文件名，直接记录，不再额外询问是否新建，也不要默认追加到 `LEARNING_NOTES.md` 或已有模块笔记。
- 只有我明确要求使用之前某份文档时，才将本次记录补充到该文档。若新笔记与已有文件同名，按本次子主题细化中文文件名，不覆盖旧文件；相关旧笔记用相对链接引用，不复制整段内容。
- 拆分粒度按知识模块确定，不按每条消息机械拆文件。一轮记录涉及多个独立知识块时可以拆成多份；同一机制中的紧密相关概念可以放在一份笔记中。
- 模块笔记保存本块知识的学习问题、原始理解、例子、校准结论、术语、来源和验证状态，并说明它承接哪个模块、输出如何进入下一环节；按实际内容组织，不为形式填空章节。
- `LEARNING_NOTES.md` 保存任务总览、模块索引、学习顺序、掌握状态和下一步；索引链接到各中文模块笔记及已有 Demo。阶段性学完后，在总笔记中串联完整流程，具体知识仍以模块笔记为主要来源，避免重复维护。

### 实现目录与模块笔记关联

- 需要从 Demo（用于理解机制的最小示例程序）入手时，在当前 Work Pool 的 `experiments/<module-slug>/` 下存放实现；这里的 `experiments/` 就是学习实现目录，沿用 W-2026-002 的结构，不再并建一个同用途的“实现”目录，也不与 `specs/implementation/` 的完成记录混淆。
- Demo 围绕一个明确的知识问题展开，按“观察输入和输出 → 追踪关键代码与状态变化 → 修改一个条件做对照 → 回到模块笔记解释机制”的节奏学习，不直接扩展成完整产品。
- 中文模块笔记通过相对链接指向相关 Demo，并说明运行入口、观察目标和实际验证状态；Demo 的 README 或入口文件说明通过相对路径指回相关模块笔记。一个模块可以关联多个 Demo，一个 Demo 也可以服务多个模块，不强制一对一。
- 只有实际需要实现或实验时才创建目录和代码。单纯要求“记下来”只记录已有讨论，不隐含要求生成、运行 Demo 或安装依赖；没有 Demo 的理论模块也可以独立成文。
- 示例结构如下，仅展示组织方式，不要求预先创建：

```text
learning-notes/work-pool/W-YYYY-NNN-readable-slug/
├─ LEARNING_NOTES.md                 # 总览、模块索引、进度与完整流程
├─ 模型运行时知识图谱.md             # 章节主链路、概念关系、扩展方案与深入入口
├─ 词嵌入与模型文件学习笔记.md        # 一个知识模块的学习过程
├─ 聊天模板与特殊Token实验笔记.md     # 另一个知识模块的学习过程
└─ experiments/                     # 按需创建的学习实现目录
   ├─ embedding-basic/              # 关联词嵌入笔记的 Demo
   └─ chat-template-basic/          # 关联聊天模板笔记的 Demo
```

### 笔记内容与维护要求

以下内容由总笔记和各模块笔记按上述职责共同承载，不要求在每份文档中重复完整章节：

1. 任务范围与状态；
2. 为什么学习它、关联章节和前置知识；
3. 本任务的学习模式；
4. 术语速查；
5. 我的原始理解、问题和伪代码；
6. 校准后的结论；
7. 调用链、数据流或状态机；
8. 来源、版本和证据；
9. 实验、指标、失败和恢复；
10. 生产级边界；
11. 掌握状态、暂缓内容和下一步。

维护时必须：

- 保留我的原始说法，不用标准答案覆盖学习过程；
- 首次出现术语时写中文含义、英文全称、所属层次和相邻概念边界；
- 区分通用原理、当前教学实现、真实源码事实和版本相关推断；
- 不把“模型说完成了”当作完成证据，不把“测试通过”自动当作业务正确；
- 对权限、租户隔离、幂等、重试、超时、竞态、审计、观测、成本、回滚和未知结果，按任务相关程度补充；
- 不把同一事实重复维护在 Work Pool、Change、章节笔记和学习笔记中；使用相对链接引用主要来源；
- 未运行的实验写“计划/未验证”，只读源码写“静态证据”，不要写成已部署或生产通过。

## 第一条回复的固定格式

正式启动后的第一条回复只需要包含：

1. 当前 Work Pool 在总学习路线中的位置；
2. 已有章节基础与仍缺的前置知识；
3. 选择理论/源码/实验/混合模式的理由；
4. 要读取的文件、源码或资料范围；
   同时给出本章 `xxx知识图谱.md` 的入口；若已创建 `名词清单.md`，一并提供入口。正文只展示简短主链路和知识块概览，不复述整张地图或完整术语表；
5. 第一小步和验收方式；
6. 一个问题，邀请我选择或直接开始第一小节。

不要一上来输出完整教程、完整源码实现或最终学习笔记。

## 完成判定

完成以约定的常见场景和常用设计主线为范围，达到上述约 80–90% 的学习目标即可，不要求穷尽所有例外。启动时将目标任务的 `Success Criteria` 对齐为“必学主线”和“选学扩展”；若原标准明确要求某项内容，先说明并与我确认范围调整，不自行省略。

只有在必学主线范围内同时满足以下条件，才说 Work Pool 学习完成：

- 我能不看资料，用自己的话解释常见场景中的核心机制、主要流程和容易混淆的相邻概念，不要求背诵严密定义或主动列全例外；
- 面对一个典型场景或小幅变化的例子，我能判断通常采用哪种常用设计，并简单说明理由；不要求独立设计完整生产系统；
- 理论任务有复述、推导或小例子；源码任务有固定版本、调用链和证据位置；实验任务有可重复输入、真实结果和至少一个失败/对照；
- 能说明教学实现、通用原理和真实生产行为分别有什么证据；
- 能指出至少一个权限、可靠性、可观测性、成本或恢复风险；
- 各中文模块笔记已保存学习过程与证据，`LEARNING_NOTES.md` 已链接相关笔记和已有 Demo，串联完整流程，并记录掌握状态、未完成项和下一步；
- 本章 `xxx知识图谱.md` 已更新概念关系、主线与扩展分支，并链接本次实际产生的子笔记和 Demo；
- 如果产生了实现或长期有效约束，再按 `specs/README.md` 更新 Change、Implementation 或 Decision；没有实现时不要为了形式创建空文档。

收尾时，将剩余的低频例外和进阶主题用几句话或简短列表列为“选学扩展”，说明何时可能用到。它们不阻塞主线完成，也不因为列出来就自动开始学习。笔记中区分“必学未完成项”和“选学扩展”：前者未完成不能宣告学完，后者可以保留。已有复述或实验足以证明掌握时，不反复追加考题或用特殊情况提高验收门槛。

## 不要自动做的事

- 不因为 Work Pool 文件存在就自动启动；
- 不把所有任务都改写成理论课，也不把所有任务都强行源码化；
- 不下载大模型、安装大依赖、启动付费 API、写入真实外部系统或修改生产代码，除非当前任务范围和用户授权明确包含；
- 不覆盖用户已有的代码、笔记、实验结果或未提交改动；
- 不把一次 Mock 成功、静态源码阅读或模型自述当作生产级验收。
