# W-2026-028：实现一个真实有用的 MCP 服务

- Status: ready
- Area: MCP / Agent Harness / Tool Integration
- Discovered From: s19 开章讨论及注释复盘；用户认为 MCP 是必要的重要内容，希望后续做一个真实有用的 MCP 服务，并补齐协议能力与 SDK 实践。
- Owner: 用户（学习与决策） / AI（指导与协作）
- Priority: high

## Objective

在理解 s19 工具发现、工具池组装与调用分发后，选择一个实际会使用的场景，完成真实 MCP Server 与客户端接入，并达到该使用场景明确约定的生产使用要求。用户已明确：目标不能止于 Demo 或连通实验，需要系统覆盖生产级工程考量，并用实现、测试和运行证据证明适用能力。

“生产级”必须落实为具体使用环境、负载、权限、可靠性目标和维护责任。以下维度都必须评估；按场景决定实现深度，不能默默省略。判定不适用的维度要记录理由、残余风险和重新评估的触发条件。

## Why Deferred / Start Trigger

当前先学习 s19，一次处理一个概念；此条目不自动启动实现。用户明确说“开始 W-2026-028”或“开始做真实 MCP 服务”时，按 [Spec 规范](../README.md) 创建 Change 并移除本 Work Pool 文件。建议启动前能复述 s19 的发现、组装、调用链。

## 后续基础协议学习清单

当前先不要求掌握协议报文和全部 MCP 能力，等进入实操前再按下面顺序补齐：

1. **角色边界**：Host、MCP Client、MCP Server、模型和下游业务系统分别负责什么；模型 Tool Calling 与 MCP 请求如何衔接。
2. **协议骨架**：JSON-RPC 请求、响应、错误、通知；初始化、版本协商、能力声明和连接关闭。
3. **核心方法**：`tools/list`、`tools/call` 的请求与返回；工具名称、描述、输入 Schema、结果和错误的边界。
4. **三类能力**：Tools（可调用动作）、Resources（可读取数据）、Prompts（可复用提示模板）；它们分别如何进入 Host 和模型上下文。
5. **传输方式**：stdio、本地子进程和远程 HTTP/Streamable HTTP；通信位置与信任边界的关系。
6. **动态发现与上下文**：工具列表变化通知、按需发现、分页、结果大小和上下文预算；理解“统一接入”不等于“全部内容自动塞给模型”。
7. **基础 Demo 到真实服务**：将 s19 的 `MCPClient.register()` / `call_tool()` 映射到真实 `tools/list` / `tools/call`，明确 mock 隐藏了哪些进程、通信和错误处理。
8. **能力模型深入**：分别学习 Tools、Resources、Prompts 的定义、发现、读取/调用、结果进入模型上下文的方式，以及它们与普通 Tool Calling、Skill 和 RAG 的边界。
9. **库与 SDK 选择**：对照官方协议、客户端 SDK、服务端 SDK/框架和测试工具；固定语言与版本后，核对 API、Transport、认证、错误、生命周期和升级策略，不把某个 SDK 的便捷封装当成协议本身。

### 必须单独掌握的生命周期与状态模型

有状态和无状态不是“HTTP 长连接与短连接”的简单同义词，而是 MCP 协议如何保存跨请求状态、如何识别一次交互、如何进行能力发现和路由的架构选择。实操前必须对照固定协议版本学习：

- 2025-11-25 及更早生命周期：initialize、notifications/initialized、能力协商和逻辑会话；理解协议会话状态与 TCP/HTTP 连接复用的区别。
- 2026-07-28 无状态核心：每个请求自描述，initialize/initialized 和 Mcp-Session-Id 的变化，server/discover、请求级元数据、负载均衡和缓存提示。
- 应用状态仍可能存在：即使 MCP 传输层无状态，长任务、幂等键、授权上下文、业务句柄和执行进度也可能由 Server 或下游系统持久化。
- 设计时要画出连接/会话状态机，说明重连、路由、缓存失效、取消、超时、恢复和多实例部署的行为。

这些内容是后续真实 MCP 实操的前置学习，不是当前章节必须一次学完的范围。当前学习阶段只保留问题清单，不预先实现协议客户端或服务端。

## 推荐学习顺序

1. 确定真实使用者、任务、数据来源，以及现有工具为何不能充分解决问题。
2. 区分 Host（宿主应用）、Client（协议客户端）、Server（能力提供方）与模型工具调用；核验官方协议和所选 SDK 的版本。
3. 定义最小工具输入、输出、错误与权限边界；候选场景是查询本仓库学习笔记、章节与 Work Pool，但尚未选定。
4. 完成真实连接、初始化、工具发现、一次实际调用与结果返回；传输方式及语言待启动时选择。
5. 验证非法参数、越权访问、断连、超时、结果过长；若包含写操作，再验证重试幂等性与审计。
6. 在实际客户端中完成一项有用任务，并对照 s19 mock 说明新增了哪些工程责任。
7. 按下表完成生产差距分析，选定量化验收目标，逐项实现适用能力、注入故障并收集证据；最后演练发布、回滚和恢复。

## 生产级考量与验收矩阵

以下是本任务的研究与验收要求，不表示 MCP 协议或任何 SDK 自动提供这些保证。启动时为每项补充：适用性、负责层、实现位置、验收方法、证据与遗留风险。

| 维度 | 必须回答的问题与设计内容 | 预期验证证据 |
| --- | --- | --- |
| 真实需求与服务目标 | 谁使用、解决什么问题、数据敏感度、负载规模；定义 SLO（服务水平目标）、延迟、错误率、成本与恢复目标 | 明确用户场景、目标值、基线与验收用例；不能用“运行成功一次”代替 |
| 协议与客户端兼容 | 固定协议/SDK 版本；协商能力、初始化、发现、分页、调用、错误；对所选传输验证适用生命周期；区分标准、扩展和宿主行为 | 协议/契约测试、目标客户端接入记录、版本兼容矩阵 |
| 工具契约与演进 | 输入输出 Schema、运行时校验、错误分类、稳定名称、规范化碰撞、版本变更与弃用；区分协议错误、业务失败和未知执行结果 | 错误参数、重复名称、输出不合约、旧客户端等案例 |
| 身份认证与凭据 | 区分用户、宿主、服务与下游身份；适用时核验 OAuth、令牌受众与范围、过期刷新和撤销；凭据存储、轮换与脱敏；不盲目透传 Token | 无凭据、伪造/过期/错误受众、撤销和轮换测试；日志无秘密 |
| 授权与租户隔离 | 每次调用的对象级权限、最小权限、租户隔离；发现阶段过滤不能替代执行阶段授权；审批绑定具体操作与参数 | 跨用户/租户/资源访问被拒绝；权限变更立即按约定生效；缓存不串数据 |
| 信任边界与攻击面 | 工具描述、annotations 和结果均不能直接充当授权；提示注入、工具投毒、路径穿越、符号链接、命令注入、SSRF；按传输处理 Origin/Host 等边界 | 威胁模型与适用攻击样本；验证服务端和下游强制限制，而非仅靠 prompt |
| 副作用与幂等 | 写入前持久化意图；稳定幂等键与去重范围；响应丢失后的结果查询/对账；事务、部分成功、补偿；不能把超时等同失败后盲重试 | “写入成功但响应丢失”、重复请求、进程重启案例；重复副作用受到控制 |
| 超时、重试与取消 | 端到端截止时间、下游超时、有限退避重试、熔断；取消如何传播，无法取消的操作如何追踪；长任务是否需持久状态与恢复 | 慢下游、断连、取消、重连故障注入；写清是否仍有后台副作用 |
| 并发与资源治理 | 并发上限、连接池、速率/配额、背压（过载时限制接收）、队列边界、资源泄漏；多实例共享状态和竞态 | 负载测试与过载行为、长时间运行资源曲线、并发一致性案例 |
| 上下文与数据量 | 工具发现不等于全部注入模型；按需选择、Schema token 预算、分页/截断/结果引用、缓存失效、数据新鲜度与权限相关缓存键 | 工具数量和大结果下的 token/延迟/选工具准确率；截断可识别、引用可恢复 |
| 进程、连接与部署 | 本地子进程的环境、权限、stdout 协议污染、退出清理；远程部署的 TLS、探针、优雅停机、扩缩容、会话/状态路由和配置校验 | 启停、重启、滚动更新、异常退出与资源清理演练；配置错误可诊断 |
| 可观测性与审计 | 关联 Agent Run、工具调用、MCP 请求和下游操作；结构化日志、指标、Trace、敏感数据脱敏、保留周期和审计权限 | 能从一次失败追踪全链；审计说明谁在何时获何种授权做了什么、结果如何 |
| 测试与效果评估 | 契约、集成、目标客户端端到端、故障、安全和性能测试；Eval（效果评估）验证工具选择与真实任务成功率 | 固定样本、可重复报告与回归门槛；区分代码通过、部署通过、任务成功 |
| 供应链与发布治理 | 依赖锁定、来源/许可证、漏洞处理、制品版本、配置迁移、灰度、禁用开关与回滚；工具定义更新的信任与兼容性 | 可追溯构建、升级前后对比、回滚演练与恢复验证 |
| 运维与持续维护 | 部署手册、告警与故障排查、维护负责人；持久状态的备份恢复、数据删除与保留；适用的隐私/合规要求 | Runbook（运维操作手册）、告警演练、适用的备份恢复实测、已知限制 |

## 责任边界与关键认知

- Tool Calling 是模型提出工具名称和参数、宿主执行并返回结果的机制，不限定本地或远程实现。
- MCP 统一宿主与能力提供方的发现和调用契约；业务 API 适配仍需实现，服务器也可以部署在本机。
- 把工具移到 MCP Server 不会自动减少模型看到的 Schema 或结果；上下文治理要另外设计和验证。
- 为每项保证标明 Host/Client、MCP Server、下游业务系统及部署平台分别负责什么；不能假设服务端能单独保证宿主正确审批或模型正确选择工具。
- MCP Resources、Prompts 等能力与 Tools 做边界对照；按需求选用，不为覆盖名词而全部实现。

## Expected Output / Success Criteria

- 一个可重复启动、具有清晰配置说明的 MCP 服务及客户端接入示例。
- 至少一个返回真实数据、解决明确问题的工具，不能仅返回固定演示字符串。
- 从发现工具到结果回到宿主的调用链说明，标清模型与程序分别负责什么。
- 正常路径及适用失败场景的验证记录，注明已验证范围和遗留问题。
- 能解释 MCP 统一接入契约与底层业务 API 实现的区别，以及工具声明与实际权限执行的区别。
- 一份完整生产验收矩阵：所有维度有评估结论，适用维度有实现与验证证据，不适用维度有理由和复审条件。
- 一份架构与信任边界图，以及关联日志、权限拒绝、故障恢复、负载和回滚的实际记录。
- 一份运行/部署/升级/故障排查手册和已知限制清单；按场景确定的 SLO 与安全验收门槛通过后，才能声明达到约定生产使用要求。
- 如阶段性只完成连通性，应明确标记为阶段成果，不以 Demo 完成作为本 Work Pool 完成。

## Boundaries / 待确认

- 业务场景、使用客户端、语言、SDK、部署位置、传输和认证方式均未选定。
- 协议报文、Tools/Resources/Prompts 的具体结构、传输和客户端选择暂缓到实操前学习；当前以 s19 基础 Demo 的工具发现和调用链为主。
- 有状态/无状态生命周期及其协议版本差异已经列为前置学习项；当前 Context7 实测使用的是 2025-11-25，不能直接代表最新协议行为。
- 本轮新增的能力模型和 SDK 学习先不展开；启动时先选一个主语言和一套官方/维护方 SDK，使用最小可运行实验验证，再做生产级扩展。
- 优先讨论最小只读场景；是否需要写入由实际需求决定。
- 公网部署、多租户、OAuth、高可用等全部纳入评估；按使用场景决定实现深度并记录依据，不将生产考量缩减为最小 Demo。
- 当前不安装依赖、不启动服务、不修改教学代码、不预写完整章节笔记。
- 主参考采用官方协议与所选官方 SDK 源码；真实产品行为必须另行核验。

## Related

- [s19 MCP 教程](../../s19_mcp_plugin/README.md)
- [s19 教学代码](../../s19_mcp_plugin/code.py)
- [W-2026-014 学习路线](./W-2026-014-agent-engineering-master-learning-roadmap.md)
- [W-2026-005 Skill 加载与治理](./W-2026-005-study-production-skill-loading-and-governance.md)：比较资源接入与 Skill 加载边界。
- [W-2026-007 副作用工具安全](./W-2026-007-study-side-effect-tool-security.md)：包含写操作时参考。
- [W-2026-010 工程化与可观测性](./W-2026-010-study-agent-skill-engineering-observability.md)：复用跨层 Trace、治理与 Eval 的学习问题。
- [W-2026-021 权限与审批治理](./W-2026-021-study-production-agent-permissions-and-approval.md)：对照身份、授权、审批与执行边界。
- [W-2026-016 Chat2DB 实践](./W-2026-016-study-chat2db-production-agent-db.md)：已有系统 MCP 接入对照，不等于本任务的服务实现。
