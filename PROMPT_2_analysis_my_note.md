# 点评我的代码学习注释

我已经在当前章节的 `code.py` 里加入了自己的理解、问题和注释。

点评前请重新读取并互相对照，不要只依赖前序对话或摘要记忆：

1. 当前章节完整的 `README.md`，包括折叠区、深入源码说明、专业术语和真实项目映射；
2. 当前章节的 `code.py`；
3. 当前章节本次 Git Diff，用它识别我实际新增或修改的注释；
4. 当前章节的 `LEARNING_NOTES.md`（如存在），了解已经确认和仍待校准的内容；
5. 与当前问题直接相关的 Spec 或 Work Pool（如存在），避免遗漏或重复登记生产扩展。

然后查看这些新增注释并逐条点评。对每个独立观点尽量使用下面的结构：

1. **我的原始理解**：保留原意，不要先替我改写成标准答案；
2. **判断**：明确标为完全正确、部分正确或错误，并只把已经确认正确的内容标成“🔴 已验证理解”；
3. **专业准确表述**：在我的直观说法基础上，给出一条专业、准确、可以直接复述或记录的工程表述；
4. **机制与分层**：说明它属于模型、Agent Loop、Harness、Tool Adapter、Runtime、业务服务或其他哪一层，以及关键调用链或状态变化；
5. **条件与边界**：指出结论成立的前提、教学实现的简化、容易混淆的相邻概念和一个最重要的生产风险；
6. **证据**：优先指出 README、当前代码或真实源码中的对应位置；涉及当前 API、SDK、框架或版本相关行为时，重新核对当前代码与官方资料，不把教程说明直接冒充当前生产事实。

点评时还需要：

- 评价哪些理解正确、部分正确或错误；
- 回答我在注释中提出的问题；
- 保留我的原始思路，补充必要的条件和边界；
- 纠正不准确的地方，并扩展我遗漏但与本章直接相关的观点；
- README 中存在直接相关的专业术语、源码说明或例子时要主动指出；如果该例子只是使用了相似技术、但消费者、目的或数据流不同，也要明确区分，不能只因都叫 `summary`、`task` 或 `memory` 就归为同一机制；
- 适合时使用简短的调用链、伪代码或用户故事帮助我理解。

这一步先只点评，不要修改代码或学习笔记。我先看看你的评价，再决定下一步如何整理和验收。
